# 변수 선언방식

## var, let, const

var은 function-scoped, let과 const는 block-scoped
var은 변수 재선언 가능, 값을 할당하지 않고 변수만 선언하는 것도 가능, 선언하기 전에 사용해도 에러가 발생하지 않는다.
let과 const는 변수 재선언 불가능 but let만 변수 재할당은 가능, 둘다 선언하기 전에 사용하면 에러가 발생한다.
